<Window x:Class="QualTrack.UI.InstructorJacketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instructor Jacket" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Icon="/QualTrackIcon.ico">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" x:Name="HeaderTextBlock"
                   Text="Instructor Jacket"
                   FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Biographical Information -->
                <GroupBox Grid.Row="0" Header="Biographical Information" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Row="0" Grid.Column="0" Content="Name:" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="NameTextBlock" Margin="10,5"/>

                        <Label Grid.Row="0" Grid.Column="2" Content="Rate:" FontWeight="Bold" Margin="20,5,0,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="3" x:Name="RateTextBlock" Margin="10,5"/>

                        <Label Grid.Row="1" Grid.Column="0" Content="DOD ID:" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="DODIdTextBlock" Margin="10,5"/>

                        <Label Grid.Row="1" Grid.Column="2" Content="Roles:" FontWeight="Bold" Margin="20,5,0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" x:Name="RolesTextBlock" Margin="10,5"/>
                    </Grid>
                </GroupBox>

                <!-- Designation Letters -->
                <GroupBox Grid.Row="1" Header="Designation Letters" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <DataGrid x:Name="DesignationDataGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  RowHeight="25"
                                  AlternatingRowBackground="LightGray"
                                  Height="140"
                                  Margin="0,5">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="80"/>
                                <DataGridTextColumn Header="Designation Date" Binding="{Binding DateDisplay}" Width="140"/>
                                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="*"/>
                                <DataGridTemplateColumn Header="PDF" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Open PDF" Click="OpenDesignationPdf_Click" Tag="{Binding}"
                                                    IsEnabled="{Binding HasPdf}" Padding="5,2" FontSize="10"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>

                <!-- Instructor Shoots -->
                <GroupBox Grid.Row="2" Header="Instructor Shoots">
                    <Grid Margin="10">
                        <DataGrid x:Name="InstructorQualificationsDataGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  RowHeight="25"
                                  AlternatingRowBackground="LightGray"
                                  Height="180"
                                  Margin="0,5">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="80"/>
                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                                <DataGridTextColumn Header="Qualification Date" Binding="{Binding DateDisplay}" Width="140"/>
                                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="*"/>
                                <DataGridTemplateColumn Header="PDF" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Open PDF" Click="OpenQualificationPdf_Click" Tag="{Binding}"
                                                    IsEnabled="{Binding HasPdf}" Padding="5,2" FontSize="10"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
